<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="movie">

	<!-- 사진 저장 -->
	<insert id="posterInsert" parameterType="com.javaex.vo.MovieVo">
	      <![CDATA[
	      	insert into movie_table
			values(null, null, null, null, null, null, null, #{ filePath }, #{ orgName }, #{ saveName }, #{ fileSize })
	      ]]>
	   </insert>

   <!-- 전체 리스트 -->
   <select id="selectMovieList" resultType="com.javaex.vo.MovieVo">
      <![CDATA[ 
        select  m_no mNo,
				m_name mName,
		        m_ticketing mTicketing,
		        m_remarks mRemarks,
		        filePath,
		        orgName,
		        saveName,
		        fileSize
		from movie_table
      ]]>
   </select>
   
</mapper>
